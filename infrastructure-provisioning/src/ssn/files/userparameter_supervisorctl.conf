UserParameter=supervisorctl.status[*],if [ "`sudo supervisorctl status $1 | tr -s ' ' | cut -f2 -d ' '`" != "RUNNING" ]; then echo "0"; else echo "1";fi
UserParameter=systemctl.status[*],if [ "`sudo systemctl is-active $1 `" != "active" ]; then echo "0"; else echo "1";fi